{
  "scripts": {
    "db:migrate": "npm --prefix server run db:migrate",
    "db:seed": "npm --prefix server run db:seed",
    "data:encrypt": "npm --prefix server run data:encrypt"
  },
  "dependencies": {
    "lucide-react": "^1.24.0",
    "react-router-dom": "^7.18.1"
  }
}
